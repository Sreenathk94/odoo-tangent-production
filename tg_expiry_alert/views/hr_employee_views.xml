<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherited the hr_payslip form view to add 'loan_line_id' field within the 'input_line_ids' tree. -->
<!--     <record id="view_employee_form" model="ir.ui.view">
        <field name="name">hr.payslip.view.form.inherit.ohrms</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id"
               ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='work_permit_expiration_date']"
                   position="after">
                <field name="passport_expire_date"/>
            </xpath>
        </field>
    </record> -->
</odoo>
